# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv312/
.pytest_cache/
.coverage
htmlcov/
*.db

# Docker
docker-compose.override.yml

# Scratch workspace used by orchestrator/scripts/capture_fixture.py - the captured
# fixtures under orchestrator/fixtures/ are the committed artifact, not this
.fixture_capture_workspace/

# Scratch workspace + telemetry logs used by orchestrator/scenarios/run_*.py demos
.scenario_runs/

# Editor/OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Env/secrets (defense in depth — this project has no API key, but keep this in place)
.env
.env.local

# Local planning/process notes — not part of the shipped system, never committed
PLANNING.md
